import type { ComponentType } from "react";
// Type surface for the platform-specific implementations
// (WeekLayout.web.tsx / WeekLayout.native.tsx). The bundler picks the right file.
export declare const WeekLayout: ComponentType;
